# PKGBUILD generated by pipman
# Python package author:  <Stéphane Caron <stephane.caron@normalesup.org>>
pkgname=python-qpsolvers
pkgver=4.12.0
pkgrel=1
pkgdesc="Quadratic programming solvers in Python with a unified API."
arch=(any)
url="https://github.com/qpsolvers/qpsolvers"
license=()
makedepends=("python" "python-pip")
build() {
  pip install --no-deps --target="qpsolvers" qpsolvers==4.12.0
}
package() {
  sitepackages=$(python -c "import site; print(site.getsitepackages()[0])")
  mkdir -p $pkgdir/"$sitepackages"
  cp -r $srcdir/qpsolvers/* $pkgdir/"$sitepackages"
}